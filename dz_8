что выведет браузер и почему?

a.
if (!("a" in window)) {
     var a = 1;
 }
 console.log(a)

выведет undefinded

b.
 var b = function a(x) {
    x && a(--x);
};
alert(a);

выдаст ошибку, потому что переменная а не объявлена

c
function a(x) {
     return x * 2;
 }
var a;
console.log(a);

выведет функцию, потому что в переменной будет ссылка на функцию

d 
function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);

выведет 10, arguments принимает в себя массив агрументов переданых
в функцию, а - это второй по индексу элемент, он заменяется на 10